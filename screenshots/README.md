# Evidências

Coloque aqui os dois prints do Discover:

1. **`01-contraste-159-para-50.png`** — a query de detecção na barra e o contador "Documents (50)" visível, mostrando a redução do ruído.
2. **`02-agrupamento-servicename.png`** — o painel de "Top values" do campo `ServiceName`, mostrando os nomes embaralhados de krbtgt (`kr1btgt`, `krbt2gt`, ...) que escaparam do filtro.

Depois de adicionar as imagens, referencie-as no README principal para que apareçam na página do repositório.
